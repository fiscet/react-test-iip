import styled from "styled-components";

const BodyContainer = styled.div`
  // This value should be stored and shared because it's used also by main-nav
  margin-left: 65px;
`;

export default BodyContainer;
